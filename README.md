# Habibi Performance

Official Habibi Performance website.

## Run locally

```bash
npm install
npm start
```

Open: http://localhost:3000

## Render

Build command:

```bash
npm install
```

Start command:

```bash
npm start
```

The vehicle power finder uses the server-side live tuning catalogue proxy.
